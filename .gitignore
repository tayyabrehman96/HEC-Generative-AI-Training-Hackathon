# Secrets — do not commit. Use Railway **Variables** or a local `.env` (untracked).
.env
.env.local
.env.*.local
node_modules/
dist/
# Server-written scan aggregates (project root only — not src/data/)
/data/
*.log
.DS_Store
Thumbs.db
.vscode/*
!.vscode/extensions.json
